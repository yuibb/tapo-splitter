# Tapo録画ファイル スプリッター v1.2.0

Tapoシリーズのカメラで録画したMP4を、カメラ映像内の日時OSD（画面表示）から読み取り、録画中に時刻がジャンプした位置で分割するPythonツールです。

フォントデータは配布物に内蔵しません。初回利用時に、利用者自身の録画からOSDテンプレートJSONを作成します。

## 注意点

分割位置はMP4のGOP（キーフレーム）単位です。そのため、指定したジャンプ位置に対して、分割ファイルの先頭・末尾が数フレーム程度前後することがあります。

元のMP4は変更しません。映像・音声を再エンコードせずストリームコピーしてMOVへ切り出します。

OSDの位置は、現在のTapo録画に合わせて画面左上の幅950px・高さ70pxに固定しています。別の解像度・機種・表示位置でOSD認識が不安定な場合は、映像に合わせてOSD範囲の設定を調整してください。

## 出力ファイル名

```
YYYYMMDD_HHMMSS_動画長さ.mov
```

- `YYYYMMDD_HHMMSS`: OSDから読み取った、その区間の開始日時
- `動画長さ`: 区間の長さを30秒単位で丸めた表示値（例: `2.5`）
- 録画停止が疑われる区間には末尾に `_Err` が付きます
- 同名ファイルがある場合は `_part02` のような連番が付きます

各MP4について、同じ出力フォルダに処理結果の一覧（`.md`）も作成します。

## バージョン履歴

- **v1.0**: イニシャルリリース
- **v1.1.0**: OSD認識に失敗した場合のアウトライン補助、数字ごとに20枚のテンプレートを収集する仕組み、設定変更モードを追加
- **v1.2.0**: Fill Scan / Robust Scanの役割を整理し、初回Builder向けの粗いSeed補助、Geometry Baseline、Width Profileの審判情報、候補35枚から多様性を考慮してElite20を選ぶ仕組みを追加

## 必要環境

### 動作確認済み環境

本プロジェクトのテスト環境で、Tapo録画MP4に対するOSD認識・時刻ジャンプ検出・GOP単位分割を確認済みです。

- macOS
- Tapo C230
- 録画解像度 2880×1620
- Python 3.10以上
- ffmpeg / ffprobe
- OpenCV（ヘッドレス版）
- NumPy

### 必要なファイル・条件

- 対応動画形式はMP4です
- OSD（日時表示）が映っている録画が必要です
- 録画フォルダと出力フォルダへの読み書き権限が必要です
- 分割後ファイルを書き出す空き容量が必要です
- 初回設定とJSON作成はターミナルで行います

### macOS

Pythonを実行でき、`pip`が使える環境を前提とします。

```bash
python3 -m pip install opencv-python-headless numpy
brew install ffmpeg
```

### Windows

Pythonを実行でき、`pip`が使える環境を前提とします。PowerShellまたはコマンドプロンプトで実行してください。

```powershell
py -m pip install opencv-python-headless numpy
winget install --id Gyan.FFmpeg.Shared -e
```

インストール後、新しいターミナルを開いて`ffmpeg`と`ffprobe`が実行できることを確認してください。

Pythonや依存ライブラリの導入に慣れていない場合は、READMEをCodexなどのAIコーディング支援へ渡し、環境確認とインストール手順を案内してもらう方法をおすすめします。

## 使い方

同じフォルダに必要なPythonファイルを置き、実行用の`run_tapo_splitter.py`を起動します。利用者が実行するのはこのファイルです。

```bash
python3 run_tapo_splitter.py
```

Windowsでは次のように起動できます。

```powershell
py run_tapo_splitter.py
```

コマンド入力なしで起動する場合は、Macでは`run_tapo_splitter.command`、Windowsでは`run_tapo_splitter.bat`をダブルクリックしてください。ランチャーは同梱または親フォルダの仮想環境を優先し、なければ通常のPythonを使用します。Macで初回だけ開けない場合は、ファイルを右クリックして「開く」を選びます。

初回起動時はターミナル上で、録画データフォルダと分割結果の出力フォルダを指定します。OSDテンプレートJSONはこのフォルダ直下の固定ファイルを使用し、存在しない場合は録画からサンプル画像を抽出して作成します。付属の`tapo_osd_seed.json`（粗い構造判別データ）は初回の仮ラベル提案だけに使われ、明らかな候補は自動採用、曖昧な候補は利用者に確認します。確認・修正した結果から、利用環境用のElite20を作成します。Seedはフォントデータではなく、通常の動画認識で毎回使うものでもありません。作成時は数字ごとに35枚以上の候補を集め、多様性を考慮して最終20枚を選びます。

既存JSONを完全に作り直す場合は、次のように`--fresh`を付けて実行します。

```bash
python3 build_tapo_osd_glyph_templates.py --fresh --output tapo_osd_glyph_templates.json
```

OSDが映っていない、または14桁の日時を切り出せない録画では、テンプレートJSONを作成できず処理を中止します。その場合はOSDが表示されている録画を用意して再実行してください。

設定ファイルに3項目が保存された後は、次回から初期設定を省略して本番の分割処理を開始します。

通常利用では設定ファイルを直接編集する必要はありません。録画フォルダ、出力フォルダ、並列処理数を変更する場合は、次のように実行します。

```bash
python3 run_tapo_splitter.py --setup
```

各項目でEnterのみを押すと現在の設定を維持します。Windowsでは`py run_tapo_splitter.py --setup`を使用してください。font JSONはプロジェクト直下で自動管理されるため、場所を指定する必要はありません。

## 動作の概要

1. 録画映像からOSD領域を切り抜き、Seedの仮ラベルまたは利用者が確認した日時文字列と一緒にテンプレートJSONを作成します。
2. そのJSONを使って録画ファイル内の日時OSDを順に読み取ります。
3. Fill Scanで白文字を認識し、失敗時はRobust Scan（局所コントラスト、アウトライン、raw-YのLuma Search）へ進みます。
4. 映像上の経過時間とOSD時刻の差を調べ、時刻が不連続になった位置をジャンプとして検出します。
5. 検出位置を基準に、MP4をGOP単位でストリームコピーして分割します。
6. 分割一覧と、録画停止が疑われる区間の情報をMarkdownで保存します。

Geometry BaselineはOSDのheightとslot positionが本来の形状かを見る補助審判で、数字を決める認識器ではありません。Width ProfileもHamming候補を別の数字へ変更せず、明らかな形状矛盾だけをvetoします。通常のOSD認識に失敗したフレームでは、局所コントラスト、続いて黒いアウトライン、最後にraw-YのLuma Searchで位置を推定して同じHamming認識へ戻します。Robust Scanで復旧したフレームは通常の成功フレームとして扱います。

## ライセンスについて

このツールは利用者の録画から作成したテンプレートJSONを使用します。特定フォントのデータを配布物へ同梱しないため、フォントライセンスに依存しない構成です。

## 利用・改変・再配布

本ソフトウェアはMIT Licenseの条件下で、個人利用・商用利用・改変・再配布を自由に行えます。

改造版を公開する場合は、元の著作権表示およびライセンス本文を保持してください。改良やバグ修正を行った場合は、ぜひPull Requestで本家へ還元してください。Pull Requestなしで個人用に改造・利用することも自由です。
